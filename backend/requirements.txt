fastapi[standard]==0.141.1
uvicorn==0.52.0
# Imported directly by app code (app/routers/documents.py), so pinned explicitly
# rather than relied on as a transitive dep of fastapi[standard].
httpx==0.28.1

# Database (M2)
sqlalchemy[asyncio]==2.0.51
asyncpg==0.31.0
alembic==1.18.5
pydantic-settings==2.14.2

# Auth (M3)
clerk-backend-api==6.0.1

# Webhooks (M4)
svix==1.99.1

# Blob storage (M5)
vercel_blob==0.4.2

# PDF parsing + chunking (M6). pymupdf4llm is AGPL-licensed.
pymupdf4llm==1.28.0
tiktoken==0.13.0

# Embeddings + vector store (M7). Official SDKs, used directly (D-002: no LangChain).
openai==2.53.0
qdrant-client==1.19.0
